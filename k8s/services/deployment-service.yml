# ─────────────────────────────────────────────────────────────────────────────
# Deployment Service — Kubernetes Deployment + Service
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-service
  namespace: autodeploy
  labels:
    app: deployment-service
    app.kubernetes.io/part-of: autodeploy-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deployment-service
  template:
    metadata:
      labels:
        app: deployment-service
    spec:
      containers:
        - name: deployment-service
          image: REPLACE_WITH_ECR_URL/autodeploy-deployment:latest
          ports:
            - containerPort: 8084
          env:
            - name: DB_NAME
              value: deployment_db
          envFrom:
            - configMapRef:
                name: autodeploy-config
            - secretRef:
                name: autodeploy-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: deployment-service
  namespace: autodeploy
  labels:
    app: deployment-service
spec:
  type: ClusterIP
  selector:
    app: deployment-service
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
